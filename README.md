HuggingFace 모델인 deepface를 사용해 두 이미지를 비교해 동일인인지 체크한다

## 필수 라이브러리 설치
```bash
# Python 3.10 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
```

## 실행
```bash
python face.py
```